import 'package:flutter/material.dart';

//constant colors

class AppColors {
  static const orange = Color.fromARGB(255, 255, 159, 10);
  static const hash = Color.fromARGB(255, 165, 165, 165);
  static const grey = Color.fromARGB(255, 165, 165, 165);
}
